
export const square = (x) => x * x;
export const add = (a, b) => a + b;
// export const subtract = (a, b) => a - b;

// const square = (x) => x * x;
// const add = (a, b) => a + b;
// const subtract = (a, b) => a - b;


// exporting default functions
// export { square, add, subtract as default }
// export { square, add, subtract as default}
// export default subtract;

export default (a, b) => a - b;


//exports - default export - named exports

// person.js
// named export isAdult(18) - true if adult, otherwise false
// namex export canDrink(18) - true if 21 and over, otherwise false

// import isAdult and canDrink
// use both printing result to the console

